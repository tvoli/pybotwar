distance/time to shell explosion
additional components:
    selectable components
    component costs
    multiple rangefinders
    distance traveled sensor
    proximity sensor
    touch sensor
debug mode (show sensors and actions)
tournament mode (run multiple battles with same robots)
cannon reload time
